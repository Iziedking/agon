# Copy to .env.local for local dev. Values prefixed NEXT_PUBLIC_ are exposed to
# the browser, which is fine for these (public endpoints and a public client key).

# Backend auth service
NEXT_PUBLIC_AUTH_URL=http://localhost:8082

# Coordinator live fanout (WebSocket)
NEXT_PUBLIC_WS_URL=ws://localhost:8788

# Circle Modular Wallets (email + passkey login). Get the client key from the
# Circle Console, enable Modular Wallets, and add this origin as an Allowed Domain.
NEXT_PUBLIC_CIRCLE_CLIENT_KEY=
NEXT_PUBLIC_CIRCLE_CLIENT_URL=https://modular-sdk.circle.com/v1/rpc/w3s/buidl
