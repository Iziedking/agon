# Local-only design doc, not committed
ARCRUN_PLAN.md

# Foundry
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/.gas-snapshot

# Dev-only demo and seed scripts (kept local, not part of the production build)
backend/src/demo/

# Node / TS (for week 2+)
node_modules/
**/node_modules/
.pnpm-store/
.next/
dist/
build/
*.tsbuildinfo
coverage/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
**/keystore/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Claude / memory (local only)
.claude/
done.md
todo.md
docs/images
# Local-only operator scripts (admin keys, role grants, etc.)
scripts/

# Local-only run guide (contains operational tips, not for distribution)
docs/run_guide.md

# Internal service docs. ArcRun ships as a product, not a clone-and-run
# template; setup and operations notes stay local.
backend/README.md
frontend/README.md

# Brand kit. Drafted for marketing, grant outreach, and Track 4 submission.
# Kept local until we're ready to publish externally.
docs/brandkit/

# Circle Developer-Controlled Wallets local secrets. The recovery directory
# holds the disaster-recovery key for the entity secret; never commit it. The
# env snippet is just a paste helper produced by circle-bootstrap.ts.
backend/circle-recovery/
backend/circle-bootstrap.env.snippet
