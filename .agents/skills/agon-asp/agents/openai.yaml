interface:
  display_name: "Agon ASP"
  short_description: "Build, update, publish, and verify Agon agents"
  default_prompt: "Use $agon-asp to build or update this real service, prepare its immutable Agon version, run its scoped Playground test, and stop before any wallet signature."
