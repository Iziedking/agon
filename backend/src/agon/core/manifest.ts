import { keccak256, stringToHex } from "viem";
export function canonicalManifestHash(value: unknown): `0x${string}` { return keccak256(stringToHex(canonicalizeManifest(value))); }
export function canonicalizeManifest(value: unknown): string { if (value===null||typeof value==='boolean'||typeof value==='number'||typeof value==='string') return JSON.stringify(value); if(Array.isArray(value)) return `[${value.map(canonicalizeManifest).join(',')}]`; if(typeof value==='object'){const o=value as Record<string,unknown>;return `{${Object.keys(o).sort().map(k=>`${JSON.stringify(k)}:${canonicalizeManifest(o[k])}`).join(',')}}`;} throw new Error('unsupported manifest value'); }
export type ManifestValidation = {ok:true}|{ok:false;code:string;message:string};
export function validateManifest(input: unknown): ManifestValidation { if(!input||typeof input!=='object'||Array.isArray(input)) return {ok:false,code:'invalid_shape',message:'manifest must be an object'}; const m=input as Record<string,unknown>; if(typeof m.endpoint!=='string'||!m.endpoint.startsWith('https://')) return {ok:false,code:'invalid_endpoint',message:'endpoint must use HTTPS'}; const tags=m.tags; if(!Array.isArray(tags)||tags.some(t=>typeof t!=='string')||new Set(tags).size!==tags.length) return {ok:false,code:'invalid_tags',message:'tags must be unique strings'}; const pricing=m.pricing as Record<string,unknown>|undefined; if(!pricing||pricing.rail!=='x402'||typeof pricing.amountUSDC!=='string'||!/^(0|[1-9]\d*)(\.\d{1,6})?$/.test(pricing.amountUSDC)) return {ok:false,code:'invalid_pricing',message:'x402 pricing metadata is required'}; const schema=m.inputSchema; if(schema&&typeof schema==='object'&&JSON.stringify(schema).match(/\$ref|remote|patternProperties|additionalProperties/)) return {ok:false,code:'unsafe_schema',message:'schema contains forbidden remote or executable keywords'}; return {ok:true}; }
